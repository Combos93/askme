<% question ||= nil %>
<% action_icon = question.answer.blank? ? 'reply' : 'pencil' %>

<div class="question">
  <div class="question-content">

    <div class="question-text text-lead">
      <%= question.text %>
    </div>

    <% if question.answer.present? %>
      <div class="question-answer text-lead">
        <%= question.answer if question.answer.present? %>
      </div>
    <% end %>

    <div class="question-date">
      <%= l(question.created_at, format: :short) %>
    </div>

    <% if question.author %>
      <%= link_to "@#{question.author.name}", user_path(question.author) %>
    <% else %>
      <%= "@Аноним" %>
    <% end %>
  </div>

  <div class="question-content">
    <%= @new_htag %>
    <% if question.qhashtags.present? %>
      <% qtags = question.qhashtags.split(" ") %>
      <% qtags.each do |tag| %>
          <%= link_to tag, hashtag_show_path(tag) %>
      <% end %>
    <% end %>

    <% if question.ahashtags.present? %>
      <% atags = question.ahashtags.split(" ") %>
      <% atags.each do |tag| %>
        <%= link_to tag, hashtag_show_path(tag) %>
      <% end %>
    <% end %>
  </div>

  <% if question.user == current_user %>
    <div class="question-actions">
      <%= link_to fa_icon(action_icon), edit_question_path(question) %>
      <%= link_to fa_icon('trash-o'), question_path(question),
                  method: :delete, data: {confirm: 'Точно удалить вопрос?'} %>
    </div>
  <% end %>
</div>
